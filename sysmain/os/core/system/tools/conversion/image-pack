#include <stdio.h>
#include <dirent.h>
#include <string.h>

int main(int c,char**v){
    FILE* out=fopen(v[2],"wb");
    DIR* d=opendir(v[1]);
    struct dirent* e;
    while((e=readdir(d))){
        fwrite(e->d_name,1,strlen(e->d_name),out);
        fputc('\n',out);
    }
    return 0;
}