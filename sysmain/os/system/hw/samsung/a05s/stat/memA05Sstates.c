// First rewritten file. This file marks the start of a real rewrite based on my knowledge!

// Ram allocation verification coming soon... in this file!

// You're free to rewrite the file if the file is Faulty

#include <stdio.h>

int main() {
   int mem_mb = 6000000000; // Memory gb integer measured in bytes. in this case, 6 billion bytes results in 6 gigabytes. This is the galaxy's A05S ram memory count.

// Put RAM Verification here:
  // Start
  // End

   switch(state) = { // Switch
     case 1:
       printf("Ram too low. OS Won't Load"); // if OS allocates too low ram.
       break;
     case 2:
       printf("Ram Detected");
       break;
     default:
       printf("RAM Memory Device 6gb LPDDR4X Working");
   }

   return 0;
}
